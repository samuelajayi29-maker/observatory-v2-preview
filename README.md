# Infrastructure series, prototype

Charts are published as standalone files (SVG and 3x PNG) with the source,
licence and compilation date printed in the image, so a downloaded chart is
citable on its own. The register carries a last-change date per row.

Data: AI Access Observatory ledger. Prototype, partial coverage.
